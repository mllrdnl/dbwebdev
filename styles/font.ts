import localFont from "next/font/local";

const msDawnRegular = localFont({ src: "./MendlSans-DawnRegular.otf" });

const rollerScript = localFont({ src: "./Rollerscript-Smooth.otf" });

export { msDawnRegular, rollerScript };
